e2eテストでレポート作成を要請されたときのGitHub Copilot用のAgent Skillを構築します。
`temp/テスト仕様書ひな型.xlsx` のExcelを`dotnet-script`等で構造解析しレポート作成に必要な作業をSkill化してください。
レポートは上記のひな型をベースに作成するのでSkillのresourcesに追加しておくのが良いです。

1. e2eテストの実行
2. Excelレポートの作成

上記の手順で動作しますが、工程で結果のスクリーンショットが必要となるため
必要があればPlaywrightのスクリーンショットの機能を使用できるようにするなどの対応が必要です。
ローカルでの利用が想定されるため「スクリーンショットシートリンク」はシートへの相対的なリンクを想定しています。
