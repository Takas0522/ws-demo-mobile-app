# 価格履歴機能 — テーブル定義

> 出典: `docs/excel/価格履歴機能_設計書.xlsx` — テーブル定義書シート

## 1. 設計書と現行実装の比較

### テーブル: `product_price_history`（商品価格履歴）

| 項目           | 設計書                       | 現行実装                | 差分         |
| -------------- | ---------------------------- | ----------------------- | ------------ |
| テーブル物理名 | `product_price_history`      | `product_price_history` | **一致**     |
| テーブル論理名 | 商品価格履歴                 | —                       | —            |
| 概要           | 商品の価格変更履歴を管理する | 同等                    | **一致**     |
| 関連テーブル   | products, users              | products, users         | **一致**     |
| データ保持期間 | 5 年間（1825 日）            | 未定義                  | **差分あり** |

### カラム定義の比較

| No  | 論理名      | 物理名             | 設計書                              | 現行 | 差分 |
| --- | ----------- | ------------------ | ----------------------------------- | ---- | ---- |
| 1   | 価格履歴 ID | `price_history_id` | INTEGER PK AUTO                     | 同左 | 一致 |
| 2   | 商品 ID     | `product_id`       | INTEGER NOT NULL FK                 | 同左 | 一致 |
| 3   | 変更前単価  | `old_price`        | INTEGER NOT NULL                    | 同左 | 一致 |
| 4   | 変更後単価  | `new_price`        | INTEGER NOT NULL                    | 同左 | 一致 |
| 5   | 変更日時    | `changed_at`       | TEXT NOT NULL DEFAULT datetime(now) | 同左 | 一致 |
| 6   | 変更者 ID   | `changed_by`       | INTEGER NOT NULL FK                 | 同左 | 一致 |
| 7   | 変更理由    | `change_reason`    | TEXT NULL                           | 同左 | 一致 |
| 8   | 作成日時    | `created_at`       | TEXT NOT NULL DEFAULT datetime(now) | 同左 | 一致 |

### CHECK 制約の比較

| No  | 制約名              | 設計書                           | 現行                             | 差分     |
| --- | ------------------- | -------------------------------- | -------------------------------- | -------- |
| 1   | `chk_old_price`     | `CHECK (old_price >= 1)`         | `CHECK (old_price >= 1)`         | **一致** |
| 2   | `chk_new_price`     | `CHECK (new_price >= 1)`         | `CHECK (new_price >= 1)`         | **一致** |
| 3   | `chk_price_changed` | `CHECK (old_price <> new_price)` | `CHECK (old_price <> new_price)` | **一致** |

### 外部キー定義の比較

| No  | カラム       | 設計書                                   | 現行 | 差分     |
| --- | ------------ | ---------------------------------------- | ---- | -------- |
| 1   | `product_id` | `products.product_id` ON DELETE RESTRICT | 同左 | **一致** |
| 2   | `changed_by` | `users.user_id` ON DELETE RESTRICT       | 同左 | **一致** |

### インデックス定義の比較

| No  | インデックス名                | 設計書                   | 現行 | 差分     |
| --- | ----------------------------- | ------------------------ | ---- | -------- |
| 1   | PK (`price_history_id`)       | 主キー                   | 同左 | **一致** |
| 2   | `idx_price_hist_product_id`   | `product_id`             | 同左 | **一致** |
| 3   | `idx_price_hist_changed_at`   | `changed_at`             | 同左 | **一致** |
| 4   | `idx_price_hist_prod_changed` | `product_id, changed_at` | 同左 | **一致** |

### トリガーの比較

| 項目          | 設計書                                 | 現行                           | 差分     |
| ------------- | -------------------------------------- | ------------------------------ | -------- |
| トリガー名    | `trg_record_price_history`             | `trg_record_price_history`     | **一致** |
| タイミング    | AFTER UPDATE OF unit_price ON products | 同左                           | **一致** |
| 条件          | WHEN OLD.unit_price <> NEW.unit_price  | 同左                           | **一致** |
| changed_by    | 0（システム）                          | 0                              | **一致** |
| change_reason | `'商品情報更新による自動記録'`         | `'商品情報更新による自動記録'` | **一致** |

## 2. テーブル定義の差分結論

**テーブル定義（DDL・制約・インデックス・トリガー）は設計書と現行実装が完全に一致している。スキーマ変更は不要。**

## 3. 新規要件: データ保持ポリシー

設計書で新たに定義されたデータ保持ポリシー。現行では未実装。

### ポリシー内容

| #   | 内容                                                                                                          |
| --- | ------------------------------------------------------------------------------------------------------------- |
| 1   | 価格履歴データの最大保存期間は **5 年間（1825 日）** とする                                                   |
| 2   | 5 年を超過したレコードは**定期バッチ処理にて物理削除**する                                                    |
| 3   | 削除バッチは**日次で実行**し、`changed_at` が基準日から 5 年超過したレコードを対象とする                      |
| 4   | 削除前には**バックアップログを出力**し、削除件数を記録する                                                    |
| 5   | 削除 SQL 例: `DELETE FROM product_price_history WHERE changed_at < datetime('now', 'localtime', '-5 years');` |

### 改修判断

データ保持ポリシーは**運用要件**であり、admin-struts アプリケーション本体の改修対象外。バッチ処理として別途開発が必要だが、本改修スコープには含めない。API 側でデフォルトの検索範囲を 5 年間とすることで対応する。
