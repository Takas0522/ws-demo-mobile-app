version: '3.8'

# SQLiteを使用するため、データベースコンテナは不要です。
# データベースファイルはアプリケーションの data/ ディレクトリに保存されます。
#
# 使用方法:
#   1. スキーマ初期化: sqlite3 data/mobile_app.db < src/database/schema/01_create_tables.sql
#   2. データ投入: sqlite3 data/mobile_app.db < src/database/seeds/02_seed_data.sql
